#include <stdio.h>
#include "../cheats.h"

fn main() -> int
{
    let p: struct Point;
    p.x = 123;
    p.y = 456;
    print_point(p);
    return 0;
}

fn print_point(p: struct Point)
{
    printf("{ .x=%d, .y=%d }\n", p.x, p.y);
}

struct Point
{
    x: int,
    y: int,
}