#include <stdio.h>
#include "../cheats.h"

fn(int main())
{
    let p : struct Point;
    p.x = 123;
    p.y = 456;
    print_point(p);
    return 0;
}

fn(void print_point(struct Point p))
{
    printf("{ .x=%d, .y=%d }\n", p.x, p.y);
}

struct Point
{
    int x;
    int y;
};